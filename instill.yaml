build:
  gpu: true
  python_version: "3.11"  # support only 3.11
  system_packages:
  python_packages:
    - torch==2.2.0
    - torchvision==0.17.0
    - sentencepiece==0.1.99
    - transformers==4.36.2
    - diffusers==0.24.0
    - accelerate==0.25.0
    - bitsandbytes==0.42.0
# the format for repo is {user_id}/{model_id}
# make sure the model id matched what you created on console
repo: admin/stable-diffusion-xl
